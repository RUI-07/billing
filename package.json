{
  "name": "billing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "prisma generate && next dev",
    "build": "prisma generate && prisma db push && next build",
    "start": "next start",
    "lint": "next lint",
    "migrate:dev": "dotenv -c -- npx prisma migrate dev",
    "migrate:prod": "dotenv -c -- npx prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "5.14.0",
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "dotenv": "^16.4.5",
    "eslint": "^8",
    "eslint-config-next": "14.2.3",
    "prisma": "^5.14.0",
    "typescript": "^5"
  }
}
